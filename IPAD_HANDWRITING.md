# iPad 手写与文件工作流

## 本次实现

画布左下角新增可折叠的“手写与文件”面板，中英文界面均可用。

- 画笔沿用原有压感；钢笔增强压力引起的粗细变化；荧光笔采用固定宽度、30% 不透明度。粗细可在 0.25–12 之间以 0.25 步进调节，支持颜色选择。
- 橡皮复用现有整笔画/对象擦除。再次点击返回当前笔触，不重置其粗细和颜色。此版本不提供像素级局部擦除。
- 开启“仅用 Pencil 写画”时，画笔/橡皮模式下单指平移、双指缩放。识别到触控笔后沿用原有自动开启笔模式的行为。笔尖接触期间忽略其他触控指针，避免手掌结束、延长笔画或擦除对象。
- 笔触存入元素的 `customData.handwritingBrush`，共用画布/SVG 输出轮廓算法；旧文件缺少该字段时沿用原有笔触。
- 面板采用 44px 触控按钮、安全区边距、随视口高度变化的滚动区域，兼顾横竖屏和分屏。

## 文件使用

1. “打开文件 / 导入”调用现有系统文件选择器。在 iPad 的“文件”中选择 iCloud Drive 或本地 `.excalidraw` 文件。已有画布内容时先提示替换，取消选择不清空画布。
2. “导出可编辑文件”等待图片加载并生成当前画布快照。存在未加载成功的图片时提示重试，避免导出缺图文件。
3. 支持文件分享的浏览器显示“存储到文件 / 分享”，可在系统分享面板选择“存储到文件”。“下载 / 另存为”作为通用出口，实际选择目录的方式由浏览器决定。
4. 导出后继续编辑，需要重新生成快照。导入文件后可继续修改笔画和对象。

此工作流不新增云存储后端，也不直接访问用户的 iCloud 账户。本机草稿、导出文件、iCloud 同步是不同状态；网页不能静默持续覆盖 iCloud 原文件。本次未移除应用原有的登录、协作等功能。

## Apple Pencil 笔杆双击

本次网页版本使用可见的橡皮切换按钮。Safari 的标准 Pointer Events 无法代替原生 `UIPencilInteraction` 笔杆双击回调，双击画布也不是该动作。

后续原生 iPad App 阶段：使用 WKWebView 复用编辑器，由原生 `UIPencilInteraction` 接收用户配置的笔杆双击动作，再通过明确的桥接接口切换工具；用 UIDocument/document picker 管理授权文件的原位保存。当前仓库未新增原生 App 或声称网页已支持硬件双击。

参考：

- https://developer.apple.com/documentation/uikit/uipencilinteraction
- https://developer.apple.com/documentation/uikit/uidocumentbrowserviewcontroller
- https://bugs.webkit.org/show_bug.cgi?id=296943

## 验证与真机验收

自动测试覆盖笔画被手掌触碰后的连续性、单指平移、双指缩放、荧光笔宽度与透明度、笔触保存后恢复、带内嵌图片的导出/导入、文件选择取消、快速擦除。相关绘图、历史记录和导出回归测试一起运行。

桌面浏览器验证 1024×768 横屏和 768×1024 竖屏下的面板、荧光笔与导出入口，并检查控制台错误。模拟手势与视口尺寸不等同于真实 iPad。

发布前仍需真机检查：

- Safari 和添加到主屏幕后，连续书写、掌托、抬笔、切换应用再返回。
- Apple Pencil 的轻压/重压笔触与橡皮切换。
- 旋转屏幕、分屏、双指缩放后的继续写画。
- 在系统分享面板保存到 iCloud Drive 和“我的 iPad”，关闭页面后重新导入并继续编辑；包含图片的文件也需验证。
